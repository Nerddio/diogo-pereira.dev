// The routes the smoke suite covers. Add to this list as M2 creates routes —
// it is the only thing that changes, because every assertion is a property
// rather than a piece of content.
export const ROUTES = ['/']