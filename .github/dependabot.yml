version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5

  # Keeps the SHA-pinned actions current. Without this, pinning means
  # never receiving security fixes for them.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
